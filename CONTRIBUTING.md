# 参与贡献指南（CONTRIBUTING）

欢迎贡献！这个仓库目标是成为 **2026 年中文互联网最系统、最能跑的大模型学习体系**。任何让读者"学得更快、跑得更通"的改动都欢迎。

## 目录结构速记

顶层目录 `00-16` 与《大模型知识地图》章节一一对应（见 [`00-知识地图/对照索引.md`](00-知识地图/对照索引.md)）。**新内容放到对应的数字目录**，别乱放。

## 你的第一步

1. Fork 本仓库，clone 到本地
2. 看 [`00-知识地图/对照索引.md`](00-知识地图/对照索引.md)，表格中 **⬜ 未完成** 的知识点都是候选题目
3. 优先做标 **★ 差异化主线** 的板块（08-RAG / 09-Agent / 11-评测），因为那是最稀缺、最涨 star 的
4. 开一个分支，命名建议：`feat/rag-agentic`、`fix/typo-kvcache`、`content/vllm-bench`
5. 提交并提 PR，PR 描述里**注明对应哪个知识点编号**（如"完成 17.6.7 Agentic RAG"）

## 文章写作规范（重要）

每篇正文 md 尽量遵循统一模板（详见 `plan.md` §2.3）：

1. **是什么**（3 句内）
2. **为什么会出现**（解决什么问题）
3. **关键概念 / 公式 / 组件**（带小表）
4. **怎么跑**（指向 `code/notebooks/` 的可运行 notebook）
5. **评测 / 验证**（跑完看什么数字）
6. **常见坑**（3–5 条）
7. **参考资料**

**内容守则**：
- 只写你验证过的内容；引用的论文 / 文档给链接
- 若参考上游 html,可见性要求请见 NOTICE；大段原文翻译须保留出处（遵守 Apache-2.0）
- 代码必须能跑，给出运行环境与依赖；优先 Colab 一键
- 不写 "AI 生成的废话"，一句顶十句

## 代码 / Notebook 提交要求

- 全部放 `code/`，命名 `不重复编号`，并同步更新 `对照索引.md` 的 notebook 列
- 提供 `requirements.txt` 增量说明（或改根目录文件）
- 用真实数据跑出至少 1 个数字 / 1 个图（没有结果 = 没完成）

## PR 提交清单

- [ ] 更新了 `对照索引.md` 对应行（✅ / 🔥）
- [ ] 代码可运行，附输出截图或数字
- [ ] 链接不是死链（CI 会检查）
- [ ] 提交信息规范：`feat:` / `docs:` / `fix:` / `content:` 前缀

## CI 说明

PR 自动跑两个检查：
- Markdown 死链检查（`.github/workflows/ci.yml`）
- 目录 -- 索引一致性检查

CI 全绿是合并前提。本地想跑死链检查：

```bash
# 需要 Node/npx
npx markdown-link-check "**/*.md" --config .markdown-link-check.json
```

## Issue 模板使用场景

- **content-request**：想学但仓库里没有的知识点 → 用这个模板，高 priority 的会被优先写
- **知识纠错**：发现技术错误 → 用这个模板（附原文与出处），纠正即贡献

有问题先搜 issue，避免重复。谢谢你的参与 🚀
